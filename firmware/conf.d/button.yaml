# Button is pin on the side of the ESP32s2
# Connected to pin 8
binary_sensor:
  - platform: gpio
    pin:
      number: 0
      mode: INPUT_PULLUP
      inverted: true
    name: "Button"
    internal: true
    state_topic:
    on_press:
      then:
        #TODO: implement factory reset on 5 second hold
